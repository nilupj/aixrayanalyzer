[server]
headless = true
address = "0.0.0.0"
port = 5000

[browser]
# Disable file watcher messages in output
gatherUsageStats = false

[client]
# Increased timeout to better handle PyTorch modules
toolbarMode = "minimal"